# Infrastructure Scaling
Use PgBouncer, read replicas, Redis hot caches, BullMQ-compatible queues, monthly partitions for event history, materialized state/category statistics and OpenTelemetry. Alert on API 5xx, scraper failures, queue backlog and DB latency.
