'use client';import { useState } from 'react';export default function Page(){const[s,setS]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/contributor-submissions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});setS(r.ok?'Submission received':'Please check the form')}return <main className="mx-auto max-w-2xl px-4 py-10"><h1 className="text-3xl font-black">Submit an update</h1><form onSubmit={submit} className="mt-6 grid gap-3 rounded border bg-white p-5"><input name="name" placeholder="Name" className="min-h-11 rounded border px-3"/><input name="email" type="email" placeholder="Email" className="min-h-11 rounded border px-3"/><input name="sourceUrl" type="url" placeholder="Official source URL" className="min-h-11 rounded border px-3"/><input name="title" required minLength={5} placeholder="Update title" className="min-h-11 rounded border px-3"/><textarea name="description" minLength={10} placeholder="Details" className="min-h-28 rounded border p-3"/><button className="min-h-11 rounded bg-primary font-bold text-white">Submit</button><p aria-live="polite">{s}</p></form></main>}
