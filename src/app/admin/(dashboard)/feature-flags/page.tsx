import { prisma } from '@/lib/prisma';export default async function Page(){const items=await prisma.featureFlag.findMany({orderBy:{key:'asc'}});return <div><h1 className="text-2xl font-bold">Feature Flags</h1><div className="mt-6 grid gap-3">{items.map(x=><div key={x.id} className="rounded border bg-white p-4"><b>{x.name}</b><p className="text-sm">{x.key} · {x.status} · {x.rollout}%</p></div>)}</div></div>}
