import { prisma } from '@/lib/prisma';export default async function Page(){const items=await prisma.apiKey.findMany({orderBy:{createdAt:'desc'}});return <div><h1 className="text-2xl font-bold">API Keys</h1><div className="mt-5 grid gap-3">{items.map(x=><div key={x.id} className="rounded border bg-white p-4"><b>{x.name}</b><p className="text-sm">Scopes: {x.scopes} · Limit: {x.rateLimit} · {x.isActive?'Active':'Disabled'}</p></div>)}</div></div>}
