import { prisma } from '@/lib/prisma';export default async function Page(){const items=await prisma.webhookEndpoint.findMany({orderBy:{createdAt:'desc'}});return <div><h1 className="text-2xl font-bold">Webhooks</h1><div className="mt-5 grid gap-3">{items.map(x=><div key={x.id} className="rounded border bg-white p-4"><b>{x.name}</b><p className="break-all text-sm">{x.url}</p><p className="text-xs">{x.events} · failures {x.failureCount}</p></div>)}</div></div>}
