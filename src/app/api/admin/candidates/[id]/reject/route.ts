import { NextResponse } from 'next/server';import { getServerSession } from 'next-auth';import { authOptions } from '@/lib/auth';import { prisma } from '@/lib/prisma';export async function POST(_r:Request,{params}:{params:{id:string}}){if(!await getServerSession(authOptions))return NextResponse.json({error:'Unauthorized'},{status:401});const item=await prisma.scrapeCandidate.update({where:{id:params.id},data:{reviewStatus:'REJECTED',reviewedAt:new Date()}});return NextResponse.json({item})}
