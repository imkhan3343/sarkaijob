import { NextResponse } from 'next/server';import { prisma } from '@/lib/prisma';import { validateApiKey } from '@/lib/api-auth';export async function GET(r:Request){const a=await validateApiKey(r,'posts:read');if(!a.ok)return a.response;const u=new URL(r.url),n=Math.min(Math.max(Number(u.searchParams.get('limit')||20),1),100),category=u.searchParams.get('category'),state=u.searchParams.get('state');const data=await prisma.postItem.findMany({where:{status:'PUBLISHED',category:category?{slug:category}:undefined,state:state||undefined},include:{category:{select:{name:true,nameHindi:true,slug:true}}},orderBy:{publishedAt:'desc'},take:n});return NextResponse.json({data})}
