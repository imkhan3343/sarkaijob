import { NextResponse } from 'next/server';import { prisma } from '@/lib/prisma';export async function GET(){const start=Date.now();try{await prisma.$queryRaw`SELECT 1`;return NextResponse.json({status:'ok',latencyMs:Date.now()-start,services:{database:'ok',redis:process.env.REDIS_URL?'configured':'disabled'}},{headers:{'Cache-Control':'no-store'}})}catch(e){return NextResponse.json({status:'error',message:e instanceof Error?e.message:'Unknown'},{status:503})}}
