import { NextResponse } from 'next/server';import { postsNeedingCheck,recordSnapshot } from '@/lib/freshness';export async function POST(r:Request){if(!process.env.CRON_SECRET||r.headers.get('x-cron-secret')!==process.env.CRON_SECRET)return NextResponse.json({error:'Unauthorized'},{status:401});const posts=await postsNeedingCheck();for(const post of posts)await recordSnapshot(post);return NextResponse.json({queued:posts.length})}
