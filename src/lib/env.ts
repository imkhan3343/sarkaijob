import { z } from 'zod';const schema=z.object({DATABASE_URL:z.string().min(1),NEXTAUTH_URL:z.string().url(),NEXTAUTH_SECRET:z.string().min(16),NEXT_PUBLIC_SITE_URL:z.string().url(),REDIS_URL:z.string().url().optional(),NEXT_PUBLIC_GA4_ID:z.string().optional(),NEXT_PUBLIC_PLAUSIBLE_DOMAIN:z.string().optional(),NEXT_PUBLIC_ADSENSE_CLIENT:z.string().optional(),SENTRY_DSN:z.string().optional(),TELEGRAM_CHANNEL_URL:z.string().url().optional(),CRON_SECRET:z.string().min(16).optional(),WEBHOOK_DISPATCH_SECRET:z.string().min(16).optional()});export const validateEnv=(input:NodeJS.ProcessEnv=process.env)=>schema.safeParse(input)
