'use client';import { useState } from 'react';export function AlertPreferences(){const[p,setP]=useState({jobs:true,results:true,admit:true});return <fieldset className="rounded border bg-white p-4"><legend className="font-bold">Alert preferences</legend>{Object.entries(p).map(([k,v])=><label key={k} className="mr-4 inline-flex gap-2"><input type="checkbox" checked={v} onChange={()=>setP({...p,[k]:!v})}/>{k}</label>)}</fieldset>}
