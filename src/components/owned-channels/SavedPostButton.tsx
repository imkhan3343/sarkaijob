'use client';import { useState } from 'react';export function SavedPostButton({slug}:{slug:string}){const[saved,setSaved]=useState(false);function toggle(){const key='saved-posts',items=JSON.parse(localStorage.getItem(key)||'[]') as string[],next=items.includes(slug)?items.filter(x=>x!==slug):[...items,slug];localStorage.setItem(key,JSON.stringify(next));setSaved(next.includes(slug))}return <button onClick={toggle} className="min-h-11 rounded border px-4 text-sm">{saved?'Saved':'Save post'}</button>}
