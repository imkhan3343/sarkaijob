import { getAdSlot } from '@/lib/ads';export async function AdSlot({slotKey,pageType,categorySlug}:{slotKey:string;pageType?:string;categorySlug?:string}){const slot=await getAdSlot(slotKey,pageType,categorySlug);if(!slot)return null;return <aside aria-label="Advertisement" className="my-4 rounded border bg-white p-3 text-center text-sm text-slate-500">{slot.fallbackText||<ins className="adsbygoogle" style={{display:'block'}} data-ad-client={slot.adClient||undefined} data-ad-slot={slot.slotId||undefined}/>}</aside>}
