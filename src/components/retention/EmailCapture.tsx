'use client';import { useState } from 'react';export function EmailCapture(){const[s,setS]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),res=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),source:'footer'})});setS(res.ok?'Subscribed':'Try again')}return <form onSubmit={submit} className="mt-3 flex gap-2"><label className="sr-only" htmlFor="lead-email">Email alerts</label><input id="lead-email" name="email" type="email" required placeholder="Email alerts" className="min-w-0 flex-1 rounded px-3 py-2 text-slate-900"/><button className="rounded bg-primary px-3 py-2 text-sm font-bold text-white">Subscribe</button><span className="sr-only" aria-live="polite">{s}</span></form>}
